# Binary Search in Go

A simple binary search implementation in Go.

## How it works

1. Generates a sorted list of 2^20 (1,048,576) random integers (0–999,999)
2. Picks a random target number
3. Performs binary search to find the target in the list

## Run

```bash
go run main.go
```
